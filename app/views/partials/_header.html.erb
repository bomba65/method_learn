<nav class="navbar navbar-default ">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand goto" href="<%= root_path %>">
        <%= image_tag("logo.png", :alt => "Method Coding School", :height => 35) %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class=""><a class="text" href="<%= courses_path %>">Курсы <span class="sr-only">(current)</span></a></li>
         <% if(current_user.present?) %>
 -        <li><a href="<%= profile_path %>" class="text">Мои курсы</a></li>
 -      <% end %>
      </ul>
      <% if(current_user.present?) %>
        <ul class="nav navbar-nav navbar-right">
                <li><div class="user">
                   <a type="button" class="profile dropdown-toggle" href="" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><img src="https://wallpaperscraft.com/image/paris_france_building_sky_street_108895_1920x1080.jpg" alt="..." class="img-circle"></a>
                   <ul class="dropdown-menu">
                     <li><a href="<%= profile_path %>">Мой профиль</a></li>

                     <li role="separator" class="divider"></li>
                     <li><%= link_to 'Выйти', destroy_user_session_path, method: :delete %></li>
                   </ul>
                </div></li>

          </ul>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li><a  class="btn btn-default loginbutton" href="<%= root_path %>" role="button">Войти</a></li>
          </ul>
        <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
