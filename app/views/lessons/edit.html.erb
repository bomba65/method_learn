<% if @lesson.errors.any? %>
  <ul>
  <% @lesson.errors.full_messages.each do |m| %>
    <li><%= m %></li>
  <% end %>
  </ul>
<% end %>

<%= form_for @lesson do |f| %>
  <%= f.text_field :name %>
  <%= f.submit "Edit" %>
<% end %>
